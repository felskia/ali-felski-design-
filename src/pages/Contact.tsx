import { motion } from "motion/react";
import ContactComponent from "../components/Contact";

export default function Contact() {
  return (
    <div className="min-h-screen bg-portfolio-gold">
      <ContactComponent />
    </div>
  );
}
